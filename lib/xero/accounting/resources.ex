# This file re-exports all the accounting sub-modules for convenience.
# Each module is defined in its own file; this is kept for documentation.
# See:
#   lib/xero/accounting/invoices.ex
#   lib/xero/accounting/contacts.ex
#   lib/xero/accounting/accounts.ex   (also contains BankTransactions, BankTransfers,
#                                       Payments, BatchPayments, CreditNotes, Overpayments,
#                                       Prepayments, PurchaseOrders, Quotes, Items,
#                                       TaxRates, TrackingCategories, ContactGroups,
#                                       Currencies, RepeatingInvoices, ManualJournals,
#                                       LinkedTransactions, Journals, Organisation,
#                                       Users, BrandingThemes, Budgets, InvoiceReminders,
#                                       PaymentServices, History)
#   lib/xero/accounting/reports.ex
